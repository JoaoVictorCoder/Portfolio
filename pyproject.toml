[project]
name = "site-refatorado"
version = "0.1.0"
description = "Refatoracao Flask + Jinja2 do iuricode.com"
requires-python = ">=3.11"
dependencies = [
  "flask>=3.1.0",
]

[tool.uv]
package = false
